#!/bin/bash

# todo Unfortunately, scripts run at the command line are not easy for an
# inexperienced user to use. The Custom Commands module allows you to create
# simple web interfaces for shell scripts and commands, so that they can be
# run from within Webmin at the click of a button.
